// Placeholder — the real memory library arrives with the capture work.
export function MemoryLibraryScreen() {
  return (
    <div className="stack">
      <h1 className="page-title">Memory box</h1>
      <p className="page-lead">Your saved memories will appear here.</p>
    </div>
  );
}
