// Placeholder — the real step-by-step wizard arrives with the capture work.
export function AddMemoryWizard() {
  return (
    <div className="stack">
      <h1 className="page-title">Add a memory</h1>
      <p className="page-lead">A gentle, step-by-step guide will live here.</p>
    </div>
  );
}
