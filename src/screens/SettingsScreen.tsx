// Placeholder — the full settings & backup screen arrives in the final pass.
export function SettingsScreen() {
  return (
    <div className="stack">
      <h1 className="page-title">Settings</h1>
      <p className="page-lead">Text size, contrast, motion, and backups will live here.</p>
    </div>
  );
}
